/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.nova
/.vscode
/.zed
.DS_Store

# Real database dumps with production PII / password hashes — never commit.
# A sanitized dump is provided at database/sample-data.sql instead.
/dri-laravel.sql
/data/*.sql

# Google OAuth service-account credentials — never commit real keys.
# Copy storage/credentials.json.example and fill in your own.
/storage/credentials.json
